# Optional override for API base URL.
# If omitted, the dashboard defaults to the live Railway API so the demo works out of the box.
# For local backend development, set: http://localhost:3001/api
VITE_API_URL=https://property-manager-production-a304.up.railway.app/api
